Point :: struct {
    X int
    Y int
}

Main :: task() {
    point := mem.NewC<Point>()
    point.X = 10
    point.Y = 20

    if point == nil {
        panic(c"allocation failed")
    }

    point.X = 30
}
