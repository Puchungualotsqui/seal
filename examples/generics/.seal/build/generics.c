// Generated by seal.
#include <stdbool.h>
#include <stdlib.h>
#include <stdint.h>
#include <stddef.h>
#include <stdio.h>
#include <assert.h>

#ifndef NULL
#define NULL ((void*)0)
#endif

typedef struct sealString {
	const unsigned char *data;
	size_t byte_len;
} sealString;

static inline uint32_t sealUtf8DecodeAdvance(const unsigned char *data, size_t byte_len, size_t *offset) {
	if (*offset >= byte_len) return 0;
	unsigned char b0 = data[*offset];
	if (b0 < 0x80) { *offset += 1; return (uint32_t)b0; }
	if ((b0 & 0xE0) == 0xC0 && *offset + 1 < byte_len) {
		uint32_t cp = ((uint32_t)(b0 & 0x1F) << 6) | (uint32_t)(data[*offset + 1] & 0x3F);
		*offset += 2;
		return cp;
	}
	if ((b0 & 0xF0) == 0xE0 && *offset + 2 < byte_len) {
		uint32_t cp = ((uint32_t)(b0 & 0x0F) << 12) | ((uint32_t)(data[*offset + 1] & 0x3F) << 6) | (uint32_t)(data[*offset + 2] & 0x3F);
		*offset += 3;
		return cp;
	}
	if ((b0 & 0xF8) == 0xF0 && *offset + 3 < byte_len) {
		uint32_t cp = ((uint32_t)(b0 & 0x07) << 18) | ((uint32_t)(data[*offset + 1] & 0x3F) << 12) | ((uint32_t)(data[*offset + 2] & 0x3F) << 6) | (uint32_t)(data[*offset + 3] & 0x3F);
		*offset += 4;
		return cp;
	}
	*offset += 1;
	return 0xFFFD;
}

static inline size_t sealString_len(sealString s) {
	size_t offset = 0;
	size_t count = 0;
	while (offset < s.byte_len) {
		(void)sealUtf8DecodeAdvance(s.data, s.byte_len, &offset);
		count += 1;
	}
	return count;
}

static inline uint32_t sealString_at(sealString s, ptrdiff_t index) {
	size_t char_len = sealString_len(s);
	ptrdiff_t resolved = index;
	if (resolved < 0) resolved = (ptrdiff_t)char_len + resolved;
	if (resolved < 0 || (size_t)resolved >= char_len) return 0;
	size_t offset = 0;
	size_t current = 0;
	while (offset < s.byte_len) {
		uint32_t cp = sealUtf8DecodeAdvance(s.data, s.byte_len, &offset);
		if (current == (size_t)resolved) return cp;
		current += 1;
	}
	return 0;
}

typedef enum sealTypeKind {
	sealType_invalid = 0,
	sealType_bool,
	sealType_int,
	sealType_uint,
	sealType_i8,
	sealType_i16,
	sealType_i32,
	sealType_i64,
	sealType_u8,
	sealType_u16,
	sealType_u32,
	sealType_u64,
	sealType_f32,
	sealType_f64,
	sealType_char,
	sealType_rawptr,
	sealType_any,
	sealType_string,
	sealType_cstring
} sealTypeKind;

typedef struct sealAny {
	sealTypeKind type;
	union {
		bool as_bool;
		intptr_t as_int;
		uintptr_t as_uint;
		int8_t as_i8;
		int16_t as_i16;
		int32_t as_i32;
		int64_t as_i64;
		uint8_t as_u8;
		uint16_t as_u16;
		uint32_t as_u32;
		uint64_t as_u64;
		float as_f32;
		double as_f64;
		uint32_t as_char;
		void * as_rawptr;
		sealString as_string;
		const char * as_cstring;
	} value;
} sealAny;

static inline sealAny sealAny_bool(bool value) { sealAny out; out.type = sealType_bool; out.value.as_bool = value; return out; }
static inline sealAny sealAny_int(intptr_t value) { sealAny out; out.type = sealType_int; out.value.as_int = value; return out; }
static inline sealAny sealAny_uint(uintptr_t value) { sealAny out; out.type = sealType_uint; out.value.as_uint = value; return out; }
static inline sealAny sealAny_i8(int8_t value) { sealAny out; out.type = sealType_i8; out.value.as_i8 = value; return out; }
static inline sealAny sealAny_i16(int16_t value) { sealAny out; out.type = sealType_i16; out.value.as_i16 = value; return out; }
static inline sealAny sealAny_i32(int32_t value) { sealAny out; out.type = sealType_i32; out.value.as_i32 = value; return out; }
static inline sealAny sealAny_i64(int64_t value) { sealAny out; out.type = sealType_i64; out.value.as_i64 = value; return out; }
static inline sealAny sealAny_u8(uint8_t value) { sealAny out; out.type = sealType_u8; out.value.as_u8 = value; return out; }
static inline sealAny sealAny_u16(uint16_t value) { sealAny out; out.type = sealType_u16; out.value.as_u16 = value; return out; }
static inline sealAny sealAny_u32(uint32_t value) { sealAny out; out.type = sealType_u32; out.value.as_u32 = value; return out; }
static inline sealAny sealAny_u64(uint64_t value) { sealAny out; out.type = sealType_u64; out.value.as_u64 = value; return out; }
static inline sealAny sealAny_f32(float value) { sealAny out; out.type = sealType_f32; out.value.as_f32 = value; return out; }
static inline sealAny sealAny_f64(double value) { sealAny out; out.type = sealType_f64; out.value.as_f64 = value; return out; }
static inline sealAny sealAny_char(uint32_t value) { sealAny out; out.type = sealType_char; out.value.as_char = value; return out; }
static inline sealAny sealAny_rawptr(void * value) { sealAny out; out.type = sealType_rawptr; out.value.as_rawptr = value; return out; }
static inline sealAny sealAny_any(sealAny value) { return value; }
static inline sealAny sealAny_string(sealString value) { sealAny out; out.type = sealType_string; out.value.as_string = value; return out; }
static inline sealAny sealAny_cstring(const char * value) { sealAny out; out.type = sealType_cstring; out.value.as_cstring = value; return out; }

static inline void seal_trap(void) {
	#if defined(__GNUC__) || defined(__clang__)
	__builtin_trap();
	#else
	abort();
	#endif
}

static inline void seal_unreachable(void) {
	#if defined(__GNUC__) || defined(__clang__)
	__builtin_unreachable();
	#else
	abort();
	#endif
}

static inline void seal_panic_empty(void) {
	fprintf(stderr, "panic\n");
	abort();
}

static inline void seal_panic_cstring(const char *message) {
	fprintf(stderr, "panic: %s\n", message ? message : "<null>");
	abort();
}

static inline void seal_panic_string(sealString message) {
	fprintf(stderr, "panic: %.*s\n", (int)message.byte_len, (const char *)message.data);
	abort();
}

typedef struct sealVariadic_bool {
	bool *data;
	size_t len;
} sealVariadic_bool;
typedef struct sealVariadic_int {
	intptr_t *data;
	size_t len;
} sealVariadic_int;
typedef struct sealVariadic_uint {
	uintptr_t *data;
	size_t len;
} sealVariadic_uint;
typedef struct sealVariadic_i8 {
	int8_t *data;
	size_t len;
} sealVariadic_i8;
typedef struct sealVariadic_i16 {
	int16_t *data;
	size_t len;
} sealVariadic_i16;
typedef struct sealVariadic_i32 {
	int32_t *data;
	size_t len;
} sealVariadic_i32;
typedef struct sealVariadic_i64 {
	int64_t *data;
	size_t len;
} sealVariadic_i64;
typedef struct sealVariadic_u8 {
	uint8_t *data;
	size_t len;
} sealVariadic_u8;
typedef struct sealVariadic_u16 {
	uint16_t *data;
	size_t len;
} sealVariadic_u16;
typedef struct sealVariadic_u32 {
	uint32_t *data;
	size_t len;
} sealVariadic_u32;
typedef struct sealVariadic_u64 {
	uint64_t *data;
	size_t len;
} sealVariadic_u64;
typedef struct sealVariadic_f32 {
	float *data;
	size_t len;
} sealVariadic_f32;
typedef struct sealVariadic_f64 {
	double *data;
	size_t len;
} sealVariadic_f64;
typedef struct sealVariadic_char {
	uint32_t *data;
	size_t len;
} sealVariadic_char;
typedef struct sealVariadic_rawptr {
	void * *data;
	size_t len;
} sealVariadic_rawptr;
typedef struct sealVariadic_any {
	sealAny *data;
	size_t len;
} sealVariadic_any;
typedef struct sealVariadic_string {
	sealString *data;
	size_t len;
} sealVariadic_string;
typedef struct sealVariadic_cstring {
	const char * *data;
	size_t len;
} sealVariadic_cstring;

typedef intptr_t Id;

typedef struct Pair_int_string {
	intptr_t first;
	sealString second;
} Pair_int_string;

typedef struct Box_Pair_int_string {
	Pair_int_string value;
} Box_Pair_int_string;

typedef struct Box_int {
	intptr_t value;
} Box_int;

typedef struct Buffer_Id_3 {
	Id data[3];
} Buffer_Id_3;

typedef struct Buffer_int_4 {
	intptr_t data[4];
} Buffer_int_4;

typedef struct Pair_string_string {
	sealString first;
	sealString second;
} Pair_string_string;


int main(void);

int main(void) {
	Box_int intBox = (Box_int){.value = 10};
	intptr_t copied = (intBox).value;
	Pair_string_string textPair = (Pair_string_string){.first = (sealString){.data = (const unsigned char *)"hello", .byte_len = 5}, .second = (sealString){.data = (const unsigned char *)"generics", .byte_len = 8}};
	Box_Pair_int_string nested = (Box_Pair_int_string){.value = (Pair_int_string){.first = 7, .second = (sealString){.data = (const unsigned char *)"nested", .byte_len = 6}}};
	Pair_int_string nestedValue = (nested).value;
	intptr_t nestedNumber = (nestedValue).first;
	sealString nestedText = (nestedValue).second;
	Buffer_int_4 numbers = (Buffer_int_4){.data = {1, 2, 3, 4}};
	intptr_t firstNumber = (numbers).data[0];
	Buffer_Id_3 ids = (Buffer_Id_3){.data = {((Id)(1)), ((Id)(2)), ((Id)(3))}};
	Id firstId = (ids).data[0];
	assert((copied == 10));
	assert((nestedNumber == 7));
	assert(((uintptr_t)(nestedText).byte_len > 0));
	assert((firstNumber == 1));
	assert((firstId == ((Id)(1))));
	return 0;
}

